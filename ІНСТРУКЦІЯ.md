# Інструкція для барбера

Коротко: що зробити, щоб бот запрацював. Технічні деталі — у `README.md`.

---

## Крок 1. Отримати токен бота

1. У Telegram знайди **@BotFather**
2. Напиши `/newbot`, вкажи назву та username бота
3. Скопіюй токен — довгий рядок виду `123456789:ABCdefGHIjklmNOPqrs...`

Якщо бот уже створений — токен можна перевипустити: `/mybots` → обрати бота → *API Token* → *Revoke current token*.

## Крок 2. Дізнатись свій Telegram ID

Напиши боту **@userinfobot** команду `/start` — він відповість числом. Це твій ID.
Саме цей ID отримає доступ до адмін-панелі.

## Крок 3. Створити файл налаштувань

У папці з ботом лежить файл **`.env.example`**.
Зроби його копію і назви просто **`.env`** (крапка на початку обов'язкова).

Відкрий `.env` звичайним блокнотом і встав свої два значення:

```
BOT_TOKEN=токен_з_кроку_1
ADMIN_IDS=твій_ID_з_кроку_2
```

Решту рядків можна не чіпати — вони мають робочі значення за замовчуванням.

> ⚠️ Файл `.env` — це ключі від бота. Не викладай його в інтернет і не надсилай нікому.
> У git він не потрапляє.

## Крок 4. Запустити

Один раз встановити бібліотеки:

```bash
pip install -r requirements.txt
```

Запустити бота:

```bash
python bot.py
```

У вікні має з'явитись `Бот @твій_бот запущено`. Тепер напиши боту `/start` у Telegram —
побачиш адмін-меню.

**Поки це вікно відкрите — бот працює. Закриєш вікно — бот зупиниться.**
Щоб він працював цілодобово, потрібен сервер — розділ «Як зробити, щоб бот працював 24/7» у `README.md`.

---

## Що вміє адмін-панель

| Кнопка | Що робить |
|---|---|
| 📋 Всі записи | усі майбутні записи; натиснути на день → на запис, щоб скасувати |
| 📊 Статистика | стрижки й заробіток за цей та минулий місяць, записи на тиждень |
| ➕ Записати клієнта | записати вручну, можна навіть на час поза графіком |
| 💰 Встановити ціну | змінити ціну; бот окремо спитає, чи повідомляти клієнтів |
| 🚫 Закрити день | прибрати всі вільні вікна на день (наявні записи залишаються) |
| 🔓 Відкрити день | повернути назад закритий день або окремі слоти |
| 🔒 Закрити слот | закрити одне конкретне вікно |
| 📢 Розсилка | повідомлення всім клієнтам, спершу покаже попередній перегляд |

З будь-якого покрокового діалогу можна вийти командою `/cancel`.

## Що бачить клієнт

**📅 Записатись** → обрати дату → обрати час → підтвердити.
**🗒 Мої записи** → переглянути або скасувати свій запис.
**💈 Ціна та графік** → актуальна вартість і години роботи.

Клієнт може мати щонайбільше **2 активні записи** одночасно і скасувати самостійно
не пізніше ніж **за 3 години** до стрижки. Ці числа міняються у `.env`
(`MAX_ACTIVE_BOOKINGS` і `CANCEL_DEADLINE_HOURS`).

## Що бот робить сам

- **9:00** — нагадування клієнтам: за день до запису і в день запису
- **9:05** — тобі приходить план на сьогодні зі списком клієнтів
- **1-го числа о 9:10** — аналітика за минулий місяць

## Графік роботи

Заданий у файлі `config.py`, змінна `WORK_SCHEDULE`. Числа `0–6` — це дні тижня,
де `0` = понеділок, `6` = неділя. Два значення — початок роботи і час останнього запису:

```python
WORK_SCHEDULE = {
    0: ("17:00", "21:20"),  # Понеділок
    5: ("14:40", "22:00"),  # Субота
}
```

Якщо день прибрати зі списку — він стане вихідним.
Тривалість однієї стрижки — `SLOT_MINUTES` у тому ж файлі (зараз 40 хвилин).

---

## Часті питання

**Бот не запускається, пише про BOT_TOKEN або ADMIN_IDS** — перевір, що файл називається
рівно `.env` (не `.env.txt`) і лежить у тій самій папці, що й `bot.py`.

**Бачу меню клієнта замість адмінського** — у `.env` вказаний не той ID. Перевір через @userinfobot.

**Бот не пише мені про нові записи** — натисни боту `/start`. Telegram не дозволяє боту
написати першим тому, хто з ним ще не спілкувався. Те саме стосується клієнтів.

**Куди дівається база клієнтів** — у файл `barber_bot.db` поруч із ботом. Це всі записи
й телефони клієнтів: роби копію файлу час від часу і нікому його не надсилай.
